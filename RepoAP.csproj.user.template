<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="Current" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <!-- Set this to the folder that contains 'REPO.exe' -->
    <REPO>D:\SteamLibrary\steamapps\common\REPO</REPO>
    <!-- Set this to the location of your BepInEx core folder -->
	  <BepInCore>$(REPO)\BepInEx\core</BepInCore>
    <!-- Set these to the folders that contain dlls for MenuLib and REPOLib -->
    <MenuLib>$(REPO)\BepInEx\plugins\nickklmao-MenuLib</MenuLib>
    <REPOLib>$(REPO)\BepInEx\plugins\Zehs-REPOLib</REPOLib>
    <!-- Set this to the location you want the mod files copied to for testing (usually a profile in a mod manager) -->
    <OutputFolder>$(REPO)\BepInEx\plugins</OutputFolder>
  </PropertyGroup>

  <PropertyGroup>
    <!-- Do Not Modify -->
    <Managed>$(REPO)\REPO_Data\Managed</Managed>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <DebuggerFlavor>ProjectDebugger</DebuggerFlavor>
  </PropertyGroup>
  <PropertyGroup>
    <ActiveDebugProfile>REPO Run</ActiveDebugProfile>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DebuggerFlavor>ProjectDebugger</DebuggerFlavor>
  </PropertyGroup>
</Project>